mutation UpdatePost($id: ID!, $input: PostUpdateInput!) {
  postUpdate(id: $id, input: $input) {
    post {
      id
      updatedAt
      title
      content
      authorId
    }
    error {
      message
    }
  }
}
