_format_version: "3.0"
_transform: true

services:
  - name: kong_serviceproyectoups
    host: localhost  
    port: 8080       
    protocol: http
    routes:
    - name: example_route 
      paths:
        - /mock
      strip_path: true