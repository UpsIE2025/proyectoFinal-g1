_format_version: "3.0"
_transform: true

services:
- host: httpbin.konghq.com
  name: kong_serviceproyectoups
  port: 80
  protocol: http
  routes:
  - name: example_route 
    paths:
    - /mock
    strip_path: true

